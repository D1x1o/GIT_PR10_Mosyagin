name: Task4
on: 
  push:
    branches:
      - master

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build
        run: dotnet build Program.cs

      - name: Run
        run: dotnet run Program.cs
